let str = "hello to the big world and you know it too when it's gonna be the end its me and you lol :)";
if(str.length > 10) {
   const value = str.slice(0, 10) + "...";
   console.log(value, str.length);
}
